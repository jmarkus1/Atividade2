const numeros = [10, 20, 30, 40, 50];

// Extrai os três primeiros elementos do array para as variáveis a, b e c
const [a, b, c] = numeros;

// Exibe os valores extraídos no console
console.log(a); // Saída: 10
console.log(b); // Saída: 20
console.log(c); // Saída: 30
