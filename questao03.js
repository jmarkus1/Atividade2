const aluno = { nome: "Jefferson", idade: 21, curso: "Sistemas para Internet" };

// Extrai as propriedades "nome" e "idade" criando variáveis de mesmo nome
const { nome, idade } = aluno;

// Exibe os valores no console
console.log(nome);  // Saída: Jefferson
console.log(idade); // Saída: 21
